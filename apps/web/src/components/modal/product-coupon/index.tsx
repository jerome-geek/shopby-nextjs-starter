import { ModalLayout, type DefaultModalLayoutProps } from '@/components/layout';

export const ProductCouponModal = (props: DefaultModalLayoutProps) => {
    return (
        <ModalLayout {...props} title='쿠폰 다운로드'>
            <div></div>
        </ModalLayout>
    );
};
