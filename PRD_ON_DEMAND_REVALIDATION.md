# PRD: On-demand Revalidation API

## 1. 개요

쇼핑몰 서비스에서 약관(`Terms`), 공지사항, 상품 상세 정보 등 정적 성격이 강하지만 긴급하게 수정이 필요한 데이터의 캐시를 즉시 갱신하기 위한 API를 구축한다.

## 2. 목적

-   **비용 절감**: 기본 캐싱 시간을 길게(예: 일주일) 설정하여 불필요한 API 호출 및 렌더링 부하를 줄인다.
-   **운영 유연성**: 데이터 수정 시 빌드나 배포 과정 없이 API 호출만으로 즉시 사용자에게 업데이트된 정보를 제공한다.
-   **성능 최적화**: 정적 렌더링(SSG)의 성능 이점을 유지하면서 데이터의 일관성을 보장한다.

## 3. 핵심 요구사항

### 3.1 기능 요구사항

-   **특정 경로 갱신**: 특정 URL 경로(예: `/terms/USE`)에 대한 캐시를 즉시 무효화하고 다음 요청 시 새로 렌더링하도록 유도한다.
-   **그룹 갱신 (Tag-based)**: 특정 태그(예: `terms`)가 부여된 모든 페이지를 한 번에 갱신할 수 있어야 한다.
-   **결과 피드백**: 갱신 성공 여부 및 처리된 시간 정보를 응답으로 반환한다.

### 3.2 보안 요구사항

-   **인증 토큰**: 무분별한 캐시 갱신 공격을 막기 위해 환경 변수에 설정된 `REVALIDATION_SECRET` 토큰을 확인한다.
-   **HTTPS 전용**: 보안을 위해 HTTPS 환경에서만 작동하도록 권장한다.

## 4. 상세 설계

### 4.1 API 엔드포인트

`GET /api/revalidate?path=/terms/USE&secret=YOUR_SECRET`

### 4.2 매개변수

-   `path` (String, Required): 갱신할 페이지의 경로.
-   `tag` (String, Optional): 갱신할 캐시 태그 (Next.js 캐시 태그 기능 사용 시).
-   `secret` (String, Required): 인증을 위한 비밀 키.

### 4.3 동작 프로세스

1. 클라이언트(관리자 시스템 등)에서 갱신 API 호출.
2. 서버에서 `secret` 검증.
3. 검증 성공 시 `res.revalidate(path)` 또는 `revalidateTag(tag)` 실행.
4. 처리 결과 반환 (200 OK 또는 오류 메시지).

## 5. 기대 효과

-   일주일 이상의 긴 `revalidate` 주기를 설정할 수 있어 Vercel Data Cache 사용량 및 비용을 90% 이상 절감 가능.
-   법규 변경 등에 의한 긴급 약관 수정 시 즉각적인 대응 가능.
